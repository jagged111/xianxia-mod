activity_jianghu_grand_gathering = {
	is_shown = {
		is_adult = yes
		is_landed = yes
		is_cultivator = yes
	}

	can_start_showing_failures_only = {
		NOT = { is_activity_type_on_cooldown = activity_jianghu_grand_gathering }
		is_available_adult = yes
		is_landed = yes
		is_cultivator = yes
	}

	is_valid = {
		scope:host = {
			is_alive = yes
			is_capable_adult = yes
			is_imprisoned = no
			is_landed = yes
			is_cultivator = yes
		}
	}

	on_invalidated = {
		invalidation_imprisoned_effect = yes
		invalidation_incapable_effect = yes
	}

	province_filter = realm
	ai_province_filter = realm
	max_province_icons = 1
	allow_zero_guest_invites = no
	open_invite = no
	cooldown = { years = 5 }

	can_be_activity_guest = {
		is_alive = yes
		is_adult = yes
		is_cultivator = yes
	}

	phases = {
		jianghu_gathering_phase_arrival = {
			is_predefined = yes
			location_source = pickable
			on_phase_active = {
				if = {
					limit = { this = scope:host }
					trigger_event = xianxia_activity.7100
					scope:activity = { progress_activity_phase_after = { days = 10 } }
				}
			}
		}

		jianghu_gathering_phase_sparring = {
			is_predefined = yes
			on_phase_active = {
				if = {
					limit = { this = scope:host }
					trigger_event = xianxia_activity.7110
					scope:activity = { progress_activity_phase_after = { days = 12 } }
				}
			}
		}

		jianghu_gathering_phase_exchange = {
			is_predefined = yes
			on_phase_active = {
				if = {
					limit = { this = scope:host }
					trigger_event = xianxia_activity.7120
					scope:activity = { progress_activity_phase_after = { days = 10 } }
				}
			}
		}

		jianghu_gathering_phase_climax = {
			is_predefined = yes
			on_phase_active = {
				if = {
					limit = { this = scope:host }
					trigger_event = xianxia_activity.7130
				}
			}
		}
	}
}
